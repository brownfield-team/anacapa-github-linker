<% csrf_token = form_authenticity_token %>

<%= react_component("OrphanCommits", prerender: false, props: {
    course_id: @course.id,
    job_list: [FixOrphanCommitsJob],
    csrf_token: csrf_token
}) %>